# X-Agent智能体开发计划

## 一、项目整体最终定型架构
    整体架构：前后端分离 + FastAPI服务层 + Harness工程管控层 + LangGraph编排核心 + LangChain组件生态 + 多分层存储 + 全量工具集 + 图表可视化 + PDF导出
    最终技术栈（全部敲定）
    •	前端UI：Chatbot-UI（Vue3），支持流式对话、文件上传、图表渲染、PDF导出
    •	后端服务：FastAPI（接口、SSE流式、鉴权、文件服务、异步任务）
    •	Agent编排核心：LangGraph（生产级状态机、分支、循环、断点续跑）
    •	Agent组件生态：LangChain（工具、记忆、RAG、模型适配）
    •	生产管控底座：自研Harness工程（安全、权限、审计、容错、参数校验、防注入）
    •	RAG架构：Agentic RAG + CRAG纠错，支持文档问答、多轮检索、事实校验
    •	记忆系统：四级分层记忆（工作记忆+短期记忆+长期结构化记忆+长期语义记忆）
    •	工具集：计算器、PDF/Word/Excel文档解析、联网搜索、Python沙箱脚本执行（Shell禁用）
    •	可视化能力：Mermaid流程图 + ECharts数据分析图表（柱状/折线/饼图）
    •	导出能力：后端Playwright全局PDF导出（支持文本+表格+图表完整导出）
    •	全套存储：PostgreSQL、Redis、Qdrant、MinIO对象存储、Loki日志
    •	可观测：LangSmith全链路追踪、日志审计、指标监控

## 二、开发总阶段划分（6大阶段，循序渐进、无遗漏）
    阶段1：环境搭建与基础设施部署（底座）
    阶段2：后端核心服务搭建（FastAPI基础服务）
    阶段3：Agent内核开发（LangGraph+分层记忆+提示词工程）
    阶段4：全套工具集接入（计算/文档/搜索/代码沙箱）
    阶段5：RAG知识库系统开发（Agentic RAG+CRAG）
    阶段6：Harness生产工程落地（安全/权限/审计/容错）
    阶段7：前端Chatbot-UI改造适配（流式+图表+PDF导出）
    阶段8：联调、压测、BUG修复、生产优化

## 三、分阶段详细可执行开发步骤
### 阶段1：基础设施部署（必做底座，100%前置）
    目标：搭建所有中间件存储，统一版本、统一地址、统一账号权限
    需要部署组件：PostgreSQL、Redis、Qdrant、MinIO、Loki、Playwright

    环境搭建与配置执行步骤:
    0.	环境搭建与配置(安装wsl2(或vmware虚拟机)->安装ubuntu-22.04->安装docker),vscode安装插件可直连wsl2修改代码
    1.	Docker环境初始化，统一docker-compose管理所有中间件(参考docker-compose.yml文件)
    2.	部署PostgreSQL：开启LangGraph checkpoint持久化、业务库、审计库
    3.	部署Redis：配置会话TTL、短期记忆缓存策略
    4.	部署Qdrant向量库：创建RAG向量集合、语义记忆集合（隔离存储）
    5.	部署MinIO对象存储：用于存储PDF/Word/Excel原始上传文件
    6.	部署Loki日志系统：统一收集后端、Agent、工具调用日志
    7.	后端安装Playwright并初始化Chromium内核（用于PDF导出）
    8.	所有中间件做连通性测试、账号密码固化、端口固化
    阶段交付物：全部中间件正常运行、可读写、日志正常采集
    参数配置文件.env：包含所有中间件的账号密码、端口、数据库连接信息,API_KEY等
    完成后执行docker-test.sh脚本，测试所有中间件是否正常运行

### 阶段2：后端FastAPI基础服务搭建
    目标：搭建前后端分离标准服务，支持会话、文件上传、SSE流式、基础鉴权
    执行步骤
    1.	初始化FastAPI项目结构（分层架构：路由层、服务层、模型层、工具层）
    2.	封装全局配置：模型KEY、中间件地址、超时时间、限流参数
    3.	实现基础接口：会话创建、会话列表、历史消息查询
    4.	实现文件上传接口：接收前端文件、存入MinIO、数据库记录元数据
    5.	封装SSE流式响应全局工具（适配Agent打字机输出）
    6.	实现基础鉴权、CORS跨域、请求限流、异常全局捕获
    7.	预留PDF导出异步接口路由（待后续开发）
    阶段交付物：可运行FastAPI服务、文件上传可用、SSE流式可用、基础接口完备

### 阶段3：Agent内核核心开发（最核心）
    目标：完成LangGraph生产级Agent、分层记忆、动态提示词工程
执行步骤
1.	废弃传统LangChain Chain架构，完全基于LangGraph StateGraph搭建
2.	定义全局Agent State状态（对话历史、工具返回、记忆缓存、错误信息）
3.	搭建Agent核心执行链路：用户输入 → 预处理 → 思考规划 → 工具调用 → 结果汇总 → 记忆写入
4.	接入四级分层记忆系统
￮	工作记忆：进程内存+上下文滑动窗口裁剪
￮	短期记忆：Redis会话摘要、实体抽取、TTL过期
￮	长期结构化记忆：PostgreSQL存储用户偏好、历史结论、关键事实
￮	长期语义记忆：Qdrant向量存储对话片段、经验总结
5.	落地新版提示词工程：结构化系统Prompt + 动态上下文注入 + 角色约束 + 输出格式强制规范
6.	开启Agent反思机制：结果自检、错误重试、幻觉抑制
7.	对接LangSmith，开启全链路追踪、节点耗时监控
8.	配置LangGraph PostgreSQL checkpoint：支持断点续跑、服务重启不丢任务
阶段交付物：基础对话Agent可用、记忆持久化生效、状态可恢复、可追踪

### 阶段4：全量工具集接入开发（Agent手脚能力）
    目标：让Agent拥有自动干活能力，全部工具受控、安全、可审计
执行步骤
1.	接入计算器工具：替换原生eval，使用安全数学表达式解析，禁止代码注入
2.	接入全文档解析工具
￮	PDF解析、Word解析、Excel表格解析
￮	绑定MinIO文件路径白名单，禁止任意路径读取
3.	接入Tavily联网搜索工具：解决模型知识截止，支持实时信息查询
4.	接入Python代码沙箱执行工具
￮	使用E2B沙箱，禁止本地宿主机执行
￮	支持数据分析、统计计算、数据处理脚本运行
5.	禁用Shell工具（生产安全红线）
6.	所有工具统一注册到LangGraph工具调度中心
7.	所有工具调用前置Harness校验、参数校验、日志审计
阶段交付物：Agent可自动调用全部工具、自动判断是否需要工具、工具调用可追溯

### 阶段5：RAG知识库系统开发（Agent知识库能力）
    目标：落地生产级Agentic RAG，解决文档问答、幻觉、多跳推理
执行步骤
1.	搭建文档入库流程：文件解析 → 清洗 → 切片 → 向量化 → Qdrant入库
2.	实现Agentic RAG架构：Agent自主判断是否检索、检索几轮、是否需要补充检索
3.	叠加CRAG纠错架构：检索结果有效性校验、无效结果丢弃、二次补搜
4.	实现混合检索：语义检索+关键词检索+元数据过滤+重排
5.	RAG结果强制绑定Prompt上下文，禁止模型编造内容
6.	RAG检索日志、命中统计、错误统计入库，用于优化迭代
阶段交付物：精准知识库问答、低幻觉、支持复杂多跳文档推理

### 阶段6：Harness工程(生产级管控)
    目标：补齐企业级生产能力，安全、可控、可审计、可自愈
执行步骤
1.	搭建Harness安全拦截层（所有Agent请求、工具调用必经）
￮	Prompt注入防护、参数清洗、脱敏
￮	工具白名单校验、权限校验
￮	文件路径安全校验、防路径穿越
2.	搭建全链路审计系统：每轮对话、每一次工具调用、每一次LLM请求全部日志落库
3.	搭建容错自愈系统：超时重试、异常降级、任务中断恢复、参数回滚
4.	实现上下文工程管控：自动token裁剪、摘要压缩、记忆过期清理、防上下文污染
5.	增加输出合规校验：禁止敏感内容、事实一致性校验
6.	增加限流、熔断、降级策略，防止大并发打垮服务
阶段交付物：完整生产级Harness底座，Agent具备企业上线资质


### 阶段7：前端Chatbot-UI定制开发（图表+PDF导出）
    目标：前端支持流式对话、表格列表渲染、双图表渲染、完整PDF导出
执行步骤
1.	部署Chatbot-UI基础版本，对接FastAPI后端SSE接口
2.	前端安装依赖：markdown-it-mermaid、echarts
3.	改造渲染引擎：        
￮	支持Markdown表格、列表原生渲染
￮	支持mermaid代码块渲染流程图、架构图
￮	支持echarts代码块解析JSON、渲染统计图表（柱状/折线/饼图）
4.	前端增加【导出PDF】按钮，绑定会话ID请求后端接口
5.	后端开发PDF导出异步接口：       
￮	读取完整会话记录
￮	组装完整HTML（含文本+表格+图表）
￮	Playwright无头浏览器渲染生成PDF
￮	返回文件流前端下载
6.	适配移动端、桌面端样式，优化流式体验
阶段交付物：可视化分析页面完备、PDF导出完整无错乱
    前端项目需要安装 Node.js 和 npm：
    # 安装 Node.js（推荐使用 nvm）
    curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
    nvm install --lts

    # 安装依赖
    cd frontend
    npm install


### 阶段8：全链路联调、压力测试、生产优化
执行步骤
1.	全链路联调：前端→FastAPI→Harness→LangGraph→工具→存储 全流程跑通
2.	BUG修复：记忆错乱、工具调用失败、图表渲染异常、PDF导出截断
3.	性能优化：token压缩优化、检索速度优化、接口响应优化
4.	压力测试：多并发会话、多文件上传、大批量导出测试
5.	安全复测：注入攻击测试、越权测试、路径穿越测试
6.	整理部署脚本、上线文档、运维手册
